# What I did today
-----------------------------------------
1. git init 
2. git add filename or 
   git add . (to add all files from working directory to staging area)
3. git commit -m "any msg to understand about the changes"
4. git push origin main 

- But while running push command I faced authentication error

#Note: If u are commiting for the 1st time from git 
u have to add remote repo 

# git remote add origin <ssh_repository-url>
# git remote -v  (check if remote repo added)

yOU have to generate ssh key from ssh-keygen 

# ssh-keygen -t ed25519 -C "your_email@example.com
# cat ~/.ssh/id_ed25519.pub  (view public key)
copy public key manually 

- went to github > settings > SSH & GPG keys > New_ssh_key> 
paste the key and u are authenticated 

checked authentication with the command
# ssh -T git@github.com

created and switched to new branch 
 git checkout May05
 staged files here thru 
 git add .
 git commit -m "msg"
 git push -u origin

 Raised pull request for merging both branches
 swithced branch to main 
 merged May05 branch to main 

 ---------------------------------------------------------------